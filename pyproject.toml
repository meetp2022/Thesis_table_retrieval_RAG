[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: marks tests that require network access (deselect with '-m not slow')",
]
addopts = "-v --tb=short"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
